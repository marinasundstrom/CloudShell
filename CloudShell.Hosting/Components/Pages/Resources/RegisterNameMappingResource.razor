@inject IStringLocalizer<SharedResource> L

<div class="type-registration">
    <div class="form-panel">
        <div class="procedure-message compact">
            @L["Name mappings are owned by DNS Zone resources. Create a DNS Zone and enable the initial name mapping option, or use MapHost(...) in the host declaration for programmatic resources."]
        </div>
    </div>
</div>
