<div class="d-grid gap-3">
    <p class="text-secondary mb-0">
        This section is added through an extendable section outlet after the
        host page is declared. A future CloudShell extension adapter can map
        extension contributions into the same core registration model.
    </p>
    <div class="alert alert-primary mb-0">
        The component is ordinary Razor content. Styling belongs to the host
        app, and custom renderers can be built on top of the core registry.
    </div>
</div>
