<div class="d-grid gap-3">
    <p class="text-secondary mb-0">
        The app layout hosts the composition root. The routed page is resolved
        to a page ID, and that context is cascaded to the menu, links, and
        section outlet.
    </p>
    <dl class="row mb-0">
        <div class="row mb-2">
            <dt class="col-sm-3 text-secondary">Core library</dt>
            <dd class="col-sm-9 mb-0">Typed IDs, registrations, builder, registry, and target resolution.</dd>
        </div>
        <div class="row">
            <dt class="col-sm-3 text-secondary">Blazor library</dt>
            <dd class="col-sm-9 mb-0">Plain Blazor components that render the registered graph.</dd>
        </div>
    </dl>
</div>
