<div class="d-grid gap-3">
    <p class="text-secondary mb-0">
        Page navigation uses normal Blazor routing. The composition registry
        provides the link target, and the layout-hosted composition root
        resolves the active page context for this route.
    </p>
    <div class="alert alert-secondary mb-0">
        The same menu, title outlet, and section outlet components work on
        both pages without CloudShell Hosting or a UI framework component
        package.
    </div>
</div>
