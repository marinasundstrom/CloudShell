<p class="text-secondary mb-3">
    The tab component renders the selected named section while keeping the page
    route stable.
</p>
<div class="alert alert-warning mb-0">
    The tab renderer lives in the Blazor composition library. This sample only
    supplies Bootstrap classes for the visual treatment.
</div>
