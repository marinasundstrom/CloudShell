# Load Balancer

This sample declares a Load Balancer resource together with other resources

```bash
dotnet run --project samples/LoadBalancer/CloudShell.LoadBalancer.csproj -- --urls http://localhost:5011
```